import React from 'react';

import Header from './../header';

import './style.css';

const Layout = () => {
  return (
    <div className="layout">
        <Header />
    </div>
  );
};

export default Layout;
